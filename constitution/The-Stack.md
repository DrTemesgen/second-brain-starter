# The Stack — which tool for which job

<!-- A routing table across every AI tool/subscription you actually have,
     not just Claude. Claude is the conductor here — the thing that reads
     this table and tells you which instrument to pick up — but it can't
     use a tool it doesn't know you have. Fill this in during onboarding
     Module 100 and keep it current as your toolkit changes. -->

| Job | Tool | Why this one | Notes |
|---|---|---|---|
| *(example)* Long-form writing in your voice | Claude (this system) | Has your voice file, your context | |
| *(example)* Quick factual lookup | {{FAST_CHEAP_TOOL}} | Don't spend a premium model on a lookup | |
| *(example)* Image / video generation | {{MEDIA_TOOL}} | | |
| *(example)* Local/offline work, no data leaving your machine | {{LOCAL_MODEL_IF_ANY}} | Free, private, good enough for a lot of drafting | Skip this row if you don't run anything local |

<!-- Delete the example rows and replace with your real toolkit. The point
     of this file isn't completeness — it's that Dispatch.md can point at a
     specific row instead of defaulting to "just use Claude for everything,"
     which is rarely the cheapest correct answer. -->

**Reviewed:** {{DATE}} — subscriptions and tools change; stale rows here
just mean Dispatch keeps recommending something you no longer have.
